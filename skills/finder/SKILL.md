# finder

Opens the current directory or matching worktree in Finder.

## Triggers

- /finder
- /f
- "open in finder"
- "show in finder"

## Instructions

Run this single command:

```bash
~/.claude/scripts/open-in-app.sh Finder
```
