# iterm2

Opens the current directory or matching worktree in iTerm2.

## Triggers

- /iterm2
- /iterm
- /it
- "open in iterm"
- "open iterm"

## Instructions

Run this single command:

```bash
~/.claude/scripts/open-in-app.sh iTerm
```
